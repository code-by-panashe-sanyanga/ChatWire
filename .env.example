# copy to .env for local overrides
SECRET_KEY=change-me-in-production
# Use * on Railway (same-origin UI + sockets). Locally keep localhost.
CORS_ORIGINS=http://localhost:5001,http://127.0.0.1:5001
PORT=5001
FLASK_DEBUG=1
